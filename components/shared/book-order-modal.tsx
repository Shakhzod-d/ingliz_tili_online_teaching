export default function ({
  selectedTeacher,
  selectedDay,
  setSelectedDay,
  selectedTime,
  setSelectedTime,
  handleSubmit,
  comment,
  setComment,
  setShowModal,
}: any) {
  return (
  
  );
}
